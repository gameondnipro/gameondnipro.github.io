<!DOCTYPE html>
<html lang="ru" data-bs-theme="light">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}База Гарантийных Обращений{% endblock %}</title>

    <link rel="apple-touch-icon" sizes="180x180" href="{{ url_for('static', filename='apple-touch-icon.png') }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ url_for('static', filename='favicon-32x32.png') }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ url_for('static', filename='favicon-16x16.png') }}">
    <link rel="manifest" href="{{ url_for('static', filename='site.webmanifest') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <style>
        :root {
            --bs-border-radius: 0.5rem;
            /* 0.5rem (8px) */
        }

        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            margin-bottom: 20px;
            border-radius: 0 0 10px 10px;
            overflow: hidden;
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            z-index: 1050;
            border-bottom: none;
        }

        .navbar .btn {
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }

        .navbar .btn:hover {

            transform: translateY(-1px) scale(1.02);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }

        .form-control,
        .btn {
            border-radius: 0.5rem;
        }

        .table-hover tbody tr:hover {

            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transform: translateY(-1px);
            background-color: var(--bs-table-hover-bg) !important;
        }

        .nav-divider {
            width: 1px;
            background-color: var(--bs-border-color);
            height: 25px;
            margin: 0 10px;
            opacity: 0.5;
        }

        body {
            padding-top: 60px;
        }

        [data-bs-theme="light"] {
            --bs-body-bg: #f7f7f9;
            --bs-bg-opacity: 0.8;
            --bs-navbar-bg: rgba(255, 255, 255, 0.85) !important;
            --bs-card-bg: #ffffff;
            --bs-box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }

        [data-bs-theme="dark"] {
            --bs-body-bg: #1e1e1e;
            --bs-bg-opacity: 0.8;
            --bs-navbar-bg: rgba(30, 30, 30, 0.9) !important;
            --bs-card-bg: #2d2d2d;
            --bs-border-color: #404040;
            --bs-body-color: #ebebeb;
        }

        .bg-purple {
            background-color: #5d3f6a !important;
            color: #ffffff !important;
        }

        .badge {
            display: inline-block;
            min-width: 80px;
            text-align: center;
            padding: 0.5em 0.8em;
        }

        .custom-popover {
            max-width: 300px;
            min-width: 250px;
            border: none !important;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2), 0 0 5px rgba(0, 0, 0, 0.1);

            --bs-popover-bg: transparent !important;

            backdrop-filter: blur(15px);
            overflow: hidden;
            padding: 0 !important;
            min-height: 250px;
            position: relative;
        }

        .popover-card-content {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 10px;

            background-color: transparent !important;
            color: #fff;
            z-index: 10;
        }

        .popover-gradient-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 180px;
            pointer-events: none;
            border-radius: 0 0 var(--bs-border-radius) var(--bs-border-radius);


            background: linear-gradient(to top,

                    rgba(247, 247, 249, 1) 0%,
                    rgba(247, 247, 249, 1) 30%,
                    rgba(247, 247, 249, 0.1) 70%,
                    rgba(247, 247, 249, 0) 100%);
        }


        [data-bs-theme="dark"] .popover-gradient-overlay {
            background: linear-gradient(to top,

                    rgba(30, 30, 30, 1) 0%,
                    rgba(30, 30, 30, 1) 30%,
                    rgba(30, 30, 30, 0.1) 70%,
                    rgba(30, 30, 30, 0) 100%);
        }

        [data-bs-theme="light"] .custom-popover .popover-card-content {
            color: #1e1e1e !important;
        }

        [data-bs-theme="light"] .custom-popover .popover-card-content small {
            color: rgba(30, 30, 30, 0.7) !important;
        }

        .custom-popover .popover-inner {
            background-color: rgba(var(--bs-body-bg-rgb), 0.95) !important;
            border-radius: var(--bs-popover-border-radius);
        }

        .custom-popover .popover-header {
            display: none;
        }

        .custom-popover .popover-body {
            background-color: transparent !important;
            padding: 0 !important;
        }

        .custom-popover .popover-arrow::before,
        .custom-popover .popover-arrow::after {
            border-color: transparent !important;
            box-shadow: none !important;
        }

        .popover-card-content {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 10px;
            background-color: transparent !important;
            color: #fff;
            z-index: 10;

        }

        .custom-popover img {
            max-width: 100%;
            height: auto;
        }

        @media (max-width: 576px) {
            .container {
                padding: 5px;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
        <div class="container">
            <a class="navbar-brand" href="{{ url_for('dashboard') }}">
                <i class="bi bi-laptop me-2"></i> ServicePC | Dnipro
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>





            <div class="collapse navbar-collapse" id="navbarNav">

                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link {% if request.endpoint == 'dashboard' %}active{% endif %}"
                            href="{{ url_for('dashboard') }}">Панель</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if request.endpoint == 'list_records' %}active{% endif %}"
                            href="{{ url_for('list_records') }}">Список Записей</a>
                    </li>
                </ul>

                <div class="d-flex align-items-center">

                    <form class="d-flex" role="search" action="{{ url_for('list_records') }}" method="get">
                        <input class="form-control form-control-sm" type="search" placeholder="Поиск по клиенту"
                            aria-label="Search" name="q"
                            value="{{ request.args.get('q', '') if request.endpoint == 'list_records' else '' }}">
                        <button class="btn btn-outline-light btn-sm ms-2" type="submit"> <i class="bi bi-search"></i>
                        </button>
                    </form>

                    <div class="nav-divider d-none d-sm-block"></div>

                    <ul class="navbar-nav flex-row ms-3 align-items-center">

                        <li class="nav-item me-2">
                            <button id="theme-toggle-btn" class="btn btn-outline-secondary btn-sm">
                                <i class="bi bi-moon-stars-fill" id="theme-toggle-icon"></i>
                            </button>
                        </li>

                        <li class="nav-item me-2">
                            <a class="btn btn-outline-secondary btn-sm" href="{{ url_for('settings_page') }}">
                                <i class="bi bi-gear-fill"></i>
                            </a>
                        </li>

                        <li class="nav-item me-2">
                            <a class="btn btn-success btn-sm text-nowrap" href="{{ url_for('add_record') }}"> <i
                                    class="bi bi-patch-plus-fill"></i> Добавить Новое Обращение
                            </a>
                        </li>

                        <li class="nav-item">
                            <a class="btn btn-info btn-sm" href="{{ url_for('faq_list') }}">
                                <i class="bi bi-question-circle-fill"></i>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>

        </div>
    </nav>

    <div class="container">
        {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
        {% for category, message in messages %}
        <div class="alert alert-{{ category }}" role="alert">
            {{ message }}
        </div>
        {% endfor %}
        {% endif %}
        {% endwith %}
        {% block content %}{% endblock %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        function toggleTheme(newTheme) {
            const html = document.querySelector('html');
            const themeToggleIcon = document.getElementById('theme-toggle-icon');

            if (newTheme === 'dark') {
                html.setAttribute('data-bs-theme', 'dark');
                localStorage.setItem('theme', 'dark');
                themeToggleIcon.classList.remove('bi-moon-stars-fill');
                themeToggleIcon.classList.add('bi-sun-fill');
            } else {
                html.setAttribute('data-bs-theme', 'light');
                localStorage.setItem('theme', 'light');
                themeToggleIcon.classList.remove('bi-sun-fill');
                themeToggleIcon.classList.add('bi-moon-stars-fill');
            }
        }

        window.addEventListener('load', () => {
            const savedTheme = localStorage.getItem('theme') || 'light';
            const popoverTriggerList = document.querySelectorAll('[data-bs-toggle="popover"]')
            const popoverList = [...popoverTriggerList].map(popoverTriggerEl => new bootstrap.Popover(popoverTriggerEl))
            toggleTheme(savedTheme);

            const themeToggleBtn = document.getElementById('theme-toggle-btn');
            if (themeToggleBtn) {
                themeToggleBtn.addEventListener('click', () => {
                    const currentTheme = document.querySelector('html').getAttribute('data-bs-theme');
                    const newTheme = currentTheme === 'light' ? 'dark' : 'light';
                    toggleTheme(newTheme);
                });
            }
        });
    </script>
    {% block scripts %}{% endblock %}
</body>

</html>